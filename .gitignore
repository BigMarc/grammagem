.build/
dist/
*.xcodeproj/xcuserdata/
.DS_Store
.swiftpm/

# Rust / Harper FFI build artifacts (regenerate with harper-ffi/build.sh).
# Cargo.lock IS committed (binary crate) for reproducible builds.
harper-ffi/target/
harper-ffi/lib/

# xcodebuild DerivedData (used only to generate the MLX metallib)
.xcode-build/
